/* ============================================
   API/[...PATH].TS — Vercel Serverless Catch-All
   Routes /api/* requests into the Express app.
   ============================================ */

import app from "./index.js";

export default app;
